# Haskell_LaTeX_slides

This LaTeX code should be built using a command which is similar to the
following;

```bash
pdflatex --shell-escape ./main.tex
```

The `--shell-escape` argument is required in order to prevent the LaTeX
